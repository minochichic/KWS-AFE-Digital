# 동료 확정 보드 (2026-08-25) — 이제 이게 기준이다

동료가 넘긴 세 파일이 **아날로그의 확정본**이다. 여기 값은 우리가 바꾸지 않는다.
바꿔야 할 이유가 생기면 동료에게 알린다.

| 파일 | 내용 |
|---|---|
| `netlists/netlist_preamp.cir` | 토폴로지. **×10 프리앰프 포함** |
| `artifacts/channel_components_v20260825_152314.csv` | 채널별 RA/R1/R2/C/C3/R7/R8 |
| `artifacts/common_components.csv` | 마이크 + 프리앰프 (전 채널 공통) |

`scripts/board_matrix.py` 가 이 BOM 을 ngspice `.ac` 로 재서
`../AFE/artifacts/filterbank_matrix_board.csv` 를 만든다. **설계 스크립트가 아니다**
— 부품은 주어진 것이고 우리는 측정만 한다.

```bash
.venv/bin/python analog/AFE_board/scripts/board_matrix.py
```

## 우리가 가정하던 것과 무엇이 다른가

**밴드는 125–5000 Hz.** 우리가 −0.3pp(잡음)로 재놨던 그 밴드다. 여기까지는 일치.

그런데 **필터뱅크 자체는 우리 `_125_5000` 뱅크가 아니다.** 두 가지가 구조적으로 다르다:

| | 우리 설계 | **동료 보드** |
|---|---|---|
| 채널 배치 | **mel 등간격** | **로그 등간격** (비율 1.27880 = 40^(1/15), 편차 0.0000%) |
| Q | 2.13 → 7.65 (상승) | **4.5 고정** (정 Q 필터뱅크) |
| 검출기 이득 R5/R4 | 4.7 | **10.0** |
| 프리앰프 | 없음 | **×10** (1 + Rpre2/Rpre3 = 1 + 90/10) |
| 검출기 op-amp | 상위 6채널 TLV9041D | **전 채널 OPA379** ✅ |

→ **`fx_band125` = 0.814 는 이 보드의 숫자가 아니다.** 밴드만 같고 필터 모양이 다르다.
재학습해야 한다.

검출기 op-amp 를 전 채널 OPA379 로 통일한 건 우리 데이터시트 결론과 일치한다
(TLV9041D 는 비보상, gain ≥ 10 V/V 에서만 안정 —
`../AFE_tuning/artifacts/opamp_gain_per_channel.md`).

## SPICE 로 잰 응답 (BOM 대조)

`.ac` 를 마이크에서 `/v_filt` 까지. 필터단은 선형이라 검출기·비교기는 모양에 영향 없다.

| ch | f_c BOM | f_c sim | err% | Q sim | ch | f_c BOM | f_c sim | err% | Q sim |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 0 | 125.0 | 125.0 | 0.0 | 4.10 | 8 | 894.0 | 875.0 | −2.1 | 4.34 |
| 1 | 159.9 | 156.2 | −2.3 | 4.23 | 9 | 1143.3 | 1156.2 | +1.1 | 4.44 |
| 2 | 204.4 | 218.8 | +7.0 | 3.14 | 10 | 1462.0 | 1468.8 | +0.5 | 4.48 |
| 3 | 261.4 | 250.0 | −4.4 | 3.74 | 11 | 1869.6 | 1875.0 | +0.3 | 4.56 |
| 4 | 334.3 | 343.8 | +2.8 | 4.38 | 12 | 2390.9 | 2406.2 | +0.6 | 4.55 |
| 5 | 427.5 | 437.5 | +2.3 | 4.20 | 13 | 3057.5 | 3031.2 | −0.9 | 4.48 |
| 6 | 546.7 | 562.5 | +2.9 | 4.31 | 14 | 3909.9 | 3875.0 | −0.9 | 4.46 |
| 7 | 699.1 | 687.5 | −1.7 | 4.26 | 15 | 5000.0 | 5031.2 | +0.6 | 4.67 |

통과대역 이득 25.4~25.9 dB (폭 0.5 dB — 이전 뱅크 1.58 dB 보다 평탄).

**저역 f_c/Q 오차는 회로가 아니라 측정 격자 탓이다.** 아래 참조.

## ⚠️ 한계 — 하위 채널은 우리 STFT 로 분해가 안 된다

Q=4.5 고정이라 대역폭이 `f_c/4.5` 로 f_c 에 비례한다. 하위 채널이 아주 좁아진다:

| ch | f_c | BW | STFT 빈(31.25 Hz) 대비 |
|---:|---:|---:|---:|
| **0** | 125 | **30.5 Hz** | **0.97 빈** |
| **1** | 156 | 36.9 Hz | 1.18 빈 |
| 2 | 219 | 69.6 Hz | 2.23 빈 |
| 15 | 5031 | 1078 Hz | 34.5 빈 |

**ch0 의 통과대역 전체가 STFT 빈 하나에 들어간다.** 게다가 STFT 창이 25 ms 라
그 자체 분해능이 ~40 Hz 로 ch0 의 30 Hz 보다 **넓다**. 즉 우리 시뮬은 원리적으로
ch0 를 실제보다 **뭉툭하게** 본다.

**실물 필터가 시뮬보다 날카롭다**는 뜻이므로, 이 방향의 오차는 정확도를
**과소평가**하는 쪽이다. 위 표의 ch2 Q=3.14(BOM 4.5) 같은 값도 회로가 아니라
격자 해상도 때문이다.

해소하려면 `n_fft` 를 키워야 하는데(512 → 1024 면 빈 15.6 Hz), 그건 STFT 프론트엔드
변경이라 별건이다. 지금은 **한계를 알고 진행**한다.

## C3 가 처음으로 실제 τ 를 준다

CLAUDE.md §2.5 는 "논문에 τ 수치가 없다, 추정일 뿐"이라고 적어놨다. 이제 값이 있다.
방전 경로는 R5+R4 = 550 kΩ (D2 가 꺼지면 v_env 가 R5→R4→v_filt 로 빠진다):

| ch | C3 | τ = (R4+R5)·C3 |
|---|---:|---:|
| 0–3 | 39 nF | **21.45 ms** |
| 4–7 | 15 nF | 8.25 ms |
| 8–11 | 5.6 nF | 3.08 ms |
| 12–15 | 2.2 nF | 1.21 ms |

(토폴로지에서 계산한 **추정**이다. 확정하려면 transient 로 재야 한다.)

**CLAUDE.md §3.2 가 τ 를 baseline 에서 뺀 근거 두 개 중 하나가 무너졌다:**
"논문에 수치가 없어 임의값이 된다" — 이제 임의값이 아니다. 그리고
"τ < window 면 효과가 거의 없다" — 하위 8채널의 τ(21.45 / 8.25 ms)는
`envelope_win_ms = 10 ms` **보다 길거나 비슷하다.** 즉 하위 채널에서는 평활이
프레임을 넘어 번진다.

→ **τ ablation 이 이제 제대로 된 질문이 됐다.** 다만 CLAUDE.md §3.2/§5 가
"baseline 에 켜지 말 것"이라고 못박고 있으므로, 켜는 것은 별도 결정 사항이다.

## 임계값: 동료가 이미 정해놨다

R7/R8 로 **Vthr 0.950~0.990 V** (검출기 정지점 0.9 V 위 50~90 mV). 채널별로 다르다.

우리 학습은 임계값을 **스스로 찾는다.** 학습이 끝나면 두 벌의 숫자가 생기고,
다르면 동료에게 알려야 한다. 그 대조는 `experiments/threshold_volts.py` 가 한다.

**단, PCB 로 얼리기 전에 남은 문제:** 채널별 임계값은 런마다 불안정하다
(정확도가 거의 같은 두 런 사이 순위상관 +0.53). 총합은 안정적이다.
한 런의 16개 숫자를 그대로 저항으로 새기면 재현되지 않는 값을 새기는 것이다.
시드 반복으로 평균·분산을 먼저 재야 한다. `../../docs/ABLATIONS.md` 참조.

## 재현 메모

- ngspice 는 CWD 의 `.spiceinit` 를 읽는다. 벤더 모델(LPV7215)이 `if()` 를 써서
  `ngbehavior=pski` 가 없으면 파싱 자체가 실패한다. `netlists/.spiceinit` 필수.
- KiCad 넷 이름의 앞 슬래시(`/v_filt`)는 ngspice **표현식 파서**가 나눗셈으로 읽는다.
  `board_matrix.py` 가 사본에서만 벗겨낸다. 동료 원본은 그대로 둔다.
