# transfer_v4 — `vthr_762b7b0c1885` (2026-08-31 수령)

동료가 회로를 클립마다 돌려 뽑은 **이진 프레임 46,546개**. 전달함수가 아니라
회로의 출력이다. `[16, ≤100]` CSV, 낱개 197 MB / tar 8.0 MB.

    python -m experiments.eval_board_frames --tag bd_base \
        --csv-root analog/AFE_board/transfer_v4/vthr_762b7b0c1885.tar.gz

macOS 에서 묶을 때는 **`COPYFILE_DISABLE=1`** 을 붙인다. 안 붙이면 파일마다
AppleDouble(`._foo`) 이 하나씩 따라 들어와 항목이 두 배(93k)가 되고, `tar tzf`
로는 bsdtar 가 숨겨서 보이지도 않는다. Python `tarfile` 만 그걸 본다.

    COPYFILE_DISABLE=1 tar czf vthr_762b7b0c1885.tar.gz vthr_762b7b0c1885/

## v3 대비 달라진 것: 12클래스가 됐다

`transfer_v3` 는 키워드 10개뿐이라 `bd_base` 의 0.825 와 같은 축에 놓을 수 없었다
(`_unknown_` 은 우리 최악 0.644, `_silence_` 는 최고 0.993 이라 둘을 빼면 숫자가
어느 쪽으로 움직일지 계산으로 안 정해진다). 이번엔 `silence/` 와 `unknown/` 이
같은 체인으로 들어와서 직접 비교가 가능하다. **이게 이 전송본의 값어치다.**

## ⚠️ `docs/request_v4` 에 대한 응답은 아니다

우리가 요청한 것은 `bd_base` 의 채널별 발화율을 맞춰달라는 것이었다.
안 맞았다 — 16채널 전부 v3 와 **0.2pp 이내로 동일**하고, 로그평균 이탈도
1.92x → 1.92x 로 그대로다. 임계값 프리셋
(`channel-groups-980-990-990-970-v1`) 이 우연히 v3 와 같은 발화율을 낸다.

    ch15   목표 35.3%   받은 2.0%   17.8배 미달

ch15 는 스윙이 16 mV 로 뱅크에서 가장 약하다는 우리 독립 측정
(`artifacts/swing_board.csv`) 과 같은 결론이고, 두 도구가 서로 다른 길로 같은
채널을 지목한 것이다.

따라서 **`bd_base` 를 이 프레임에 그대로 태운 정확도는 회로의 실력이 아니다.**
남의 임계값으로 만든 이미지를 우리 네트워크가 얼마나 견디는가일 뿐이고, 같은
이식을 이미 두 번 쟀다 (`bd_base_trip_s0` 0.734, `bd_nom0.1_trip_s0` 0.669 —
둘 다 우리 엔벨로프 파이프라인은 공유한 채였는데도). 회로의 실력은 재학습해야
나온다.

## 확인한 것

| | |
|---|---|
| 행 순서 | **역순** (행0 = ch15). 매니페스트에 적혀 있고, 발화율이 550–700 Hz 에서 꼭짓점을 이루는 것으로 물리 확인 — 뒤집지 않으면 125 Hz 가 가장 조용하다는 뜻이 된다 |
| AGC | **없다**. `silence` 클립별 발화율이 7.7%→75.8% 로 퍼져 레벨 변화가 살아 있다. 트랙 2 고정 임계 그대로다 |
| 죽은 채널 | 없다 (2.0~21.9%) |
| 경계 | `v_env` — R7/R8/U4 는 빠져 있고 비교기는 소프트웨어로 적용됐다 |
| 입력 | 5.0 mVpp (프리앰프 뒤) |
| 열 수 | 39~100, 소스 길이를 따른다. 짧은 것은 **뒤를** 0 으로 채운다 |

`silence` 가 45% 로 단어들(9~20%)의 4배인데 AGC 가 아니다: 활성 구간만 재면
12클래스 전부 39.6~49.2% 로 같고, 차이는 듀티뿐이다 (단어 22~40%, 소음 95%).

## 동료에게 물어볼 것

1. **`detector_valid: false`** — `export_settings.json` 이 스스로 draft 로 표시했다.
   16채널 전부 "Detector 파형/DC/피크 미통과", "보장 Vthr band 없음".
   필터는 `filter_valid: true` 인데 검출기 모델이 미검증이다. 무슨 뜻인지.
2. **`unknown/` 이 `backward_0000.csv` 로 재명명**돼 화자 해시가 사라졌다. 공식
   화자 분할을 복원할 수 없어 파일명 해시로 나눴고, 같은 화자가 train 과 test 에
   동시에 들어갈 수 있다. unknown 정확도가 0.644 보다 크게 높으면 회로가 아니라
   이 누수를 먼저 의심할 것.
3. ch15 의 16 mV 스윙 — 여전히 시스템의 단일 고장점이다. 채널별 검출기 이득이나
   더 낮은 최상단 f_c 로 30 mV 위로 올릴 수 있는지.
