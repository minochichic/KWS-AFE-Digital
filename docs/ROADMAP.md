# 앞으로 할 일 — 단일 목록

흩어진 항목을 **의존 순서**로 정리한다. 규칙 하나: **재학습이 필요 없는 것부터.**
그 결과가 어떤 학습을 돌릴지 정하므로, 순서를 뒤집으면 100 에폭을 헛돌게 된다.

**확정**: `configs/base.yaml` 이 곧 baseline 이다 — spice + sqrt + **`xlse`** + k=1.
다이오드-OR 은 max 가 아니라 soft max 를 만들고, 그 쪽이 **+6.7pp** 높다.
**δ = 0 도 확정**됐다 (§6e). 남은 미확정은 `lse_temp_frac` 하나뿐이다 (평탄대 13–50 mV).

작업 노트북은 [`notebooks/lab.ipynb`](../notebooks/lab.ipynb) 하나만 쓴다.
각 실험이 **회로를 정하는지 / FPGA를 정하는지 / 학습만의 것인지**는
[`EXPERIMENT_MAP.md`](EXPERIMENT_MAP.md).

---

## 지금 어디에 있나

| | test | 비고 |
|---|---:|---|
| 64ch 연속 log-mel (모델 상한) | 0.9198 | 모델은 병목이 아니다 |
| 16ch 연속 (프론트엔드 천장) | 0.8617 | 정규화로는 여기까지 |
| **`af_lse078` (xlse, k=1)** | **0.8445** | **현재 최고** — 천장에 −1.7pp |
| `af_lse078_s2` (seed 1235) | 0.8331 | ⚠️ 아래 P1-b 참조 |
| `af_k2` (xmix 하드 max, k=2) | 0.8024 | 비교기를 2배 써도 이보다 낮다 |
| `af_k1_ref` (xmix, k=1) | 0.7778 | 직접 비교 기준선 |

**목표 85%까지 약 1.0pp.** 소프트 max가 하드 max를 **+6.7pp** 이겼고 부품은 0개 늘었다.

---

## P0 — 재학습 없이 지금 (각 몇 분, 순서 무관)

넷 다 기존 체크포인트만 읽는다. **이 결과가 P2의 목록을 정한다.**

| # | 무엇 | 답하는 질문 | 셀 | 결과 |
|---|---|---|---|---|
| **P0-a** | frac 스윕 | 하나의 모델이 음량 범위를 견디나 | §6d | ⚠️ **낙폭 27.4pp** |
| P0-b | 레벨 보정 | GSC 폭이 배치의 25 dB를 덮나 | §6c | ✅ 앵커 건강, 23.1 dB |
| P0-c | 창 위치 곡선 | 슬라이딩의 순수 오정렬 비용 | §6a | ✅ **3.6pp — 공짜** |
| P0-d | `floor_frac` 탐색 | δ를 어디 앉히나 | §6e | ✅ **δ = 0 확정** |

**P0 전부 끝났다.** 결과 요약:

### P0-c — 슬라이딩은 공짜, 그런데 **배경잡음이 비싸다**

| | 값 | |
|---|---:|---|
| 원본 클립 | 0.8445 | 기준 |
| 재배치 + 무음 채움 | 0.7831 | −6.2pp ← **재배치 자체의 비용** |
| 재배치 + 실제 방 잡음 | 0.6846 | **−9.9pp 추가** ← 배경 도메인 시프트 |
| **위치에 따른 낙폭** | **3.6pp** | ← **우리가 재려던 것. 작다** |

위치는 값싸다(≤5pp) → **최대 확신도 선택 + 불응기만** 붙이면 된다. 긴 캔버스 증강 불필요.

**그런데 `room` 채움이 −9.9pp다.** 이건 "잡음 유무"가 아니라 **잡음 종류의 불일치**다 —
GSC 클립 자체의 잡음은 조용한 방 히스인데, `_background_noise_`는 설거지·수돗물·
운동자전거다. **처음 보는 배경 종류가 10pp를 먹는다.** → **P2-d 우선순위 대폭 상승.**

### P0-a — 게인 증강이 필요하다 (낙폭 27.4pp)

+12 dB에서 −5.8pp, **−12 dB에서 −10.3pp.** 조용한 쪽이 더 나쁘다.
→ `aug_gain_db [-12, +12]`.

#### 왜 이 측정이 필요했나

§6의 frac 표(0.13~5.5)는 평평해서 "스윙 스펙이 헐렁하다"로 읽히지만, **그 표의 각 점은
서로 다른 모델**이다. 배치에는 모델이 하나뿐이고 그 하나가 화자 음량에 따라 frac 전체를
만난다. `lse_temp`가 버퍼라 테스트 시점에 바꿀 수 있으므로 재학습 없이 잰다.

| 낙폭 | 뜻 | 다음 |
|---|---|---|
| < 5pp | 앵커 무관, 0.8445 그대로 유효 | P2에서 게인 증강 **불필요** |
| ≥ 5pp | 그 값은 **한 음량에서만** 성립 | P2-a 필수 |

### P0-c 읽는 법

| `noise` 낙폭 | 다음 |
|---|---|
| ≤ 5pp | 슬라이딩 공짜 — 최대 확신도 선택만 붙이면 끝 |
| 5–15pp | 판정 규칙 필요 (P3-b) |
| ≥ 15pp | **긴 캔버스** 증강을 만들 가치 있음 |

---

## P1 — 측정 신뢰도 (P2 전에 반드시)

둘 다 **비교 자체를 오염**시키므로 여기를 먼저 고치지 않으면 P2 결과를 못 믿는다.

### P1-a. δ — ✅ 분산은 고쳤고, ⚠️ 편향은 P0-d 로 넘어갔다

`init_fixed_scale`이 **배치 하나(128클립)**로 2% 분위수를 추정했다. 중앙값(→ T)은
1.6%밖에 안 변하는데 **2% 분위수(→ δ)는 0.00100 ↔ 0.00244로 144% 변했다.**

**✅ 고침**: `run()`이 `collect_init_batch(tr, 2048)`로 모아 넘기고, 학습 전에
**겹치지 않는 두 절반**의 δ를 찍어 아직 흔들리면 바로 보여준다. init STFT는 청크로
계산해 메모리 피크를 잡았다(값은 비트 단위 동일).

**⚠️ 남은 것 — 분산이 아니라 편향이다.** frac 스윕 7런 전부 δ가 **`sqrt(1e-6)` 가드와
정확히 같은 값**이었다. 그 가드는 **atom**이다(무음·zero padding이 전부 한 값에 쌓임).
무음이 **2.5%**인데 `floor_frac`이 **2.0%**를 요구하니 quantile이 atom **안**에 있다 —
표본을 늘리면 **가드 값으로 안정적으로 수렴할 뿐**이다. 재현은 되지만 zero padding을
잰 값이라 회로 스펙으로 못 쓴다. → **P0-d (§6e)** 가 후보를 좁힌다.

(`xlse`는 d를 *빼기* 때문에 §4-4의 발화 폭주는 안 생긴다. 학습은 무사하고,
**동료에게 넘길 수 없다는 것**만 문제다. δ의 의미: [`EXPERIMENT_MAP.md`](EXPERIMENT_MAP.md) §A-3)

### P1-b. 시드가 다르면 **테스트 세트도 다르다**

unknown 서브샘플과 합성 silence가 둘 다 seed로 뽑힌다
([data/speech_commands.py:162,166](../data/speech_commands.py)) — 테스트 클립의 **약
20%**가 시드에 따라 바뀐다.

즉 `af_lse078`(0.8445, seed 1234)과 `af_lse078_s2`(seed 1235)는 **서로 다른 테스트
세트**에서 잰 값이다. "런간 변동 0.94pp"에는 모델 변동과 **테스트 세트 변동**이 섞여
있다. 백필의 `_TE` 캐시 버그는 고쳤고(같은 seed끼리만 공유), 앞으로 시드 비교를 할 때는
**테스트 세트를 고정**하거나 이 항을 분리해야 한다.

---

## P2 — 정확도 (학습, 하나씩)

**P0/P1 결과가 어느 것을 돌릴지 정한다.** 전부 100 에폭.

| # | 실험 | 조건 | 근거 |
|---|---|---|---|
| **P2-a** | 게인 증강 | P0-a 낙폭 ≥ 5pp 또는 P0-b 폭 부족 | `xlse`에서 게인 = `lse_temp_frac`. 예전 −9pp 기각은 **고정 임계** 얘기라 여기 적용 안 됨 |
| P2-b | `spice_gain_restore: True` | 항상 해볼 값어치 | 채널별 threshold가 흡수한다는 논리는 **하드 max에서만** 참. LSE 분모는 16채널을 섞는다 |
| P2-c | k=2 + `xlse` | 전력 여유가 있으면 | 하드 max의 +2.5pp가 소프트 max와 **겹치는지 미검증** |
| **P2-d** | **배경잡음 증강** | **P2-a 와 동급** | **P0-c 에서 −9.9pp 측정.** 처음 보는 배경 종류에 취약하다 |
| P2-e | frac 재확정 | P1-a 이후 | 0.78 스파이크는 **철회**됨. 평탄 구간 13~50 mV만 유효 |
| ~~P2-f~~ | ~~`floor_frac` 스윕~~ | — | ❌ **불필요.** §6e 가 끝냈다 — 정규화값 변화 0.6pp 미만이라 스윕은 잡음을 잰다 |

> **P2-e 주의**: 0.78이 이웃(1.2 / 2.0 = 둘 다 0.826)보다 1.8pp 튄 것은 봉우리가 아니라
> 스파이크였고 시드 재현에서 무너졌다. **33 mV를 설계 목표로 확정하지 말 것.**
> 동료에게 줄 것은 "평탄 구간 13~50 mV"이지 한 점이 아니다.

---

## P3 — 배치 (슬라이딩 창)

| # | 무엇 | 상태 |
|---|---|---|
| P3-a | 웨이크온이벤트 폐기 → 자유 구동 슬라이딩 | ✅ 결정됨 (상대 임계는 프레임 95.2%에서 발화) |
| P3-b | 판정 규칙 — **최대 확신도** + 불응기 | ⬜ P0-c 결과 뒤 |
| P3-c | 시간 이동 증강 | ❌ **끝. 실패** (+0.33pp, 잘림이라 학습으로 못 고침) |

---

## P4 — 하드웨어 전달 (동료)

| # | 산출물 | 막는 것 |
|---|---|---|
| P4-a | **δ = V_ref 오프셋** | ✅ **δ=0, V_ref = 정지점 917.8 mV** |
| P4-b | α ×16 → 저항비 BOM | 최종 런 확정 (§8 셀에 준비됨) |
| P4-c | 요구 엔벨로프 스윙 (mV 범위) | **P2-e** |
| P4-d | 다이오드-OR + 버퍼 회로 | ✅ [`proposal/THRESHOLD.md`](../proposal/THRESHOLD.md) — 단 `xmix` 기준이라 `xlse`로 갱신 필요 |

---

## P5 — FPGA (KC705)

| # | 무엇 | 상태 |
|---|---|---|
| P5-a | 블록 다이어그램 | ✅ [`proposal/artifacts/fpga_blocks.svg`](../proposal/artifacts/fpga_blocks.svg) |
| P5-b | `export/` — BN→정수 threshold, bit-packing | ✅ `export/fuse.py`, `export/pack.py` |
| P5-c | 전체 모델 워커 → `$readmemh` ROM + manifest | ⬜ |
| P5-d | **골든 벡터** (층별 중간 활성) | ⬜ RTL 검증에 필수 |
| P5-e | RTL | ⬜ P5-c/d 뒤 |

리소스는 문제가 아니다: 10 Hz에 **0.6 MAC/cycle**이면 되고 접은 설계가 KC705의
**1.3%**다. 설계 압력은 용량이 아니라 **검증 용이성**이다.

---

## P6 — 문서 (아무 때나, 그러나 곧)

| # | 무엇 | 왜 |
|---|---|---|
| P6-a | **CLAUDE.md §5** | "RTL 작성 금지", "FPGA 보드 미확정" — **둘 다 무효** (KC705 확정, RTL 범위 내) |
| P6-b | `proposal/THRESHOLD.md` | `xmix`(하드 max) 기준으로 쓰여 있다. 확정 형태가 `xlse`면 갱신 |
| P6-c | 노트북 정리 | `gpu_local` / `workbench`는 보존용. **작업은 `lab`에서만** |

---

## 한 줄 요약

> **P0 끝. 다음은 학습 두 개다** — **게인 증강**(±12 dB)과 **배경잡음 증강**.
> 둘 다 P0가 숫자로 요구했다: 음량 27.4pp, 배경 9.9pp.
> 그 뒤 P3(판정 규칙) → P5(FPGA).
>
> 각 실험이 회로/FPGA/학습 중 무엇을 정하는지는 [`EXPERIMENT_MAP.md`](EXPERIMENT_MAP.md).
