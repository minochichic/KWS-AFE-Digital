# 문제 3 — FPGA 하드웨어 최적화 (folding 등)

## 3.1 출발점: 현재 모델의 실제 비용

`BinaryMatchboxNet.describe()` 출력 기준 (C=64, T=128, separable):

| 스테이지 | 파라미터 | 정밀도 | 가중치 메모리 |
|---|---:|---|---:|
| conv1 | 22,784 | int8 | 22.3 KB |
| b1 | 23,616 | binary | 2.9 KB |
| b2 | 10,624 | binary | 1.3 KB |
| b3 | 10,880 | binary | 1.3 KB |
| conv2 | 10,432 | binary | 1.3 KB |
| conv3 | 16,640 | int8 | 16.3 KB |
| conv4 | 1,548 | fixed(16b 가정) | 3.0 KB |
| **합계** | **96,524** (BN 2,560 포함) | (이진 57.6%) | **47.55 KB** (BN은 임계로 융합) |

> **첫 번째 발견**: **이진 스테이지는 파라미터의 58%지만 메모리는 겨우 ~14%**(6.8 KB).
> **int8 prologue/epilogue(conv1+conv3)가 메모리의 80%**(38.6 KB)를 먹는다.
> → 메모리 최적화 타깃은 이진 블록이 아니라 **conv1/conv3**이다.
> (참고: Hello Edge DS-CNN-S는 전체 38.6 KB로 94.4% — 우리 int8 두 층만 한 크기.)

## 3.1b 연산량 정밀 카운트 ✅ (실측, `scripts/count_ops.py`)

실제 모델에 훅을 걸어 텐서 shape에서 직접 센 값 (T=128, C=64, separable):

| 정밀도 | MACs | 비중 | 하드웨어 |
|---|---:|---:|---|
| binary (XNOR+popcount) | 3,424,256 | 56.9% | LUT |
| int8 | 2,490,368 | 41.4% | ↓ 분해 |
| fixed (conv4) | 98,304 | 1.6% | DSP |
| **합계** | **6,012,928** | | **6.01 MMAC/분류** |

### 세 가지 비직관적 발견

1. **conv1은 MAC의 24%(1.44 M)를 먹지만 곱셈기가 필요 없다.**
   입력이 ±1이라 int8 가중치의 **부호 있는 누산**(CLAUDE.md 3.3). →
   **진짜 곱셈기가 필요한 건 conv3+conv4의 1,146,880 MAC = 전체의 19.1%뿐.**
2. **이진 연산의 84.2%가 pointwise**(2.88 M) — depthwise는 15.8%(0.54 M).
   비트팩킹 이득이 큰 쪽이 압도적 다수 → **팩킹 투자 대비 효과가 크다.**
   (CLAUDE.md 3.4대로 두 PE는 분리 유지.)
3. **가중치 메모리 47.55 KB 중 int8이 38 KB(80%)** — 이진은 6.5 KB(14%).
   → 연산은 이진이 지배, **메모리는 int8이 지배**한다.

## 3.2 두 번째 발견: 완전펼침은 이 문제에 과대 설계 ✅ (정량화됨)

- KWS 분류율 **2–10 Hz**, FPGA @100 MHz → **분류당 1e7(10 Hz)–5e7(2 Hz) 사이클**.
- 필요 연산 **6.01 MMAC** → 실시간을 맞추려면 **0.12–0.60 MAC/cycle**이면 충분.
- → **단일 MAC 엔진으로도 실시간이 된다.**

완전펼침(fully-unrolled)은 시간축으로 **~1e6–1e7배 과대**하다. CLAUDE.md 5의
"완전 펼침 가정"은 **재검토 대상**이다.

### folding 설계점 (실측 사이클, @100 MHz)

세 엔진을 **독립적으로** folding (하드웨어가 다르므로):
`P_bin`=XNOR/popcount 레인, `P_acc`=부호누산 레인(conv1), `P_dsp`=진짜 int8 곱셈기.

| 설계 | P_bin | P_acc | P_dsp | bin | acc | dsp | 합계 cyc | 지연 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| **minimal** | 8 | 1 | 1 | 428 k | **1,442 k** | 1,147 k | 3,017 k | **30.2 ms** |
| **balanced** | 64 | 8 | 4 | 54 k | 180 k | 287 k | 520 k | **5.2 ms** |
| fast | 256 | 32 | 16 | 13 k | 45 k | 72 k | 130 k | 1.3 ms |

예산: **100 ms @10 Hz**, 500 ms @2 Hz.

- **가장 작은 엔진(minimal)조차 30 ms로 예산의 1/3** — 실시간 여유 3×.
- minimal에서는 **conv1의 부호누산(1.44 M cyc)이 병목**이지 이진층이 아니다.
  → P_acc를 먼저 늘리는 게 효율적(레인 1개당 소형 가산기).
- **balanced(P=64/8/4)면 5.2 ms**로 20× 여유 — 여기가 실용적 출발점.

| 방식 | 장점 | 단점 | 이 프로젝트 적합성 |
|---|---|---|---|
| **완전펼침** | 가중치를 LUT에 하드코딩(메모리 0), 최고 처리량 | 면적 폭발, 99.99% 유휴 | ❌ 과대 |
| **folding(재사용)** | 면적 최소, 자원↔지연 조절 | 가중치 메모리(BRAM) 필요, 제어 복잡 | ✅ **권장** |

**folding 설계 축** (FINN 스타일): PE(출력 병렬) × SIMD(입력 병렬)를 낮게 잡아 면적을 줄이고
남는 시간을 쓴다. 시간 여유가 압도적이므로 **가장 작은 엔진 + 최대 folding**이 출발점.

## 3.3 이진 연산 최적화 (CLAUDE.md 3.4와 일치)

- **BN → 정수 임계 융합**: 학습 후 `floor(β'/γ')` 하나로 접어 비교 1회. 곱셈기 제거. **필수**.
- **비트 팩킹 + XNOR/popcount**: `2·popcount(XNOR) − N`. popcount는 LUT6 압축기 트리.
- **depthwise ≠ pointwise**: 팩킹 이득은 **pointwise(1×1)** 에서 극대. depthwise는 채널을
  안 섞어 이득이 작다 → **두 PE를 통합하지 말 것**.
- **conv1은 곱셈기 불필요**: 입력이 ±1이라 int8 가중치의 **부호 있는 누산**으로 구현.
  (conv3만 진짜 int8×int8 → DSP 필요.)
- **residual 정수 누산**: 마지막 pointwise·skip은 α 미적용 정수 유지, 임계 1회.

## 3.4 세 번째 발견: µW 목표에서 진짜 적은 **정적 전력**

듀티비가 극단적이다 — 분류 1회에 1e7 사이클 중 실제 계산은 극히 일부, 나머지는 유휴.

- **동적 전력은 작고, 누설/정적 전력이 지배**한다.
- 일반 SRAM FPGA의 정적 전력은 **수 mW~수십 mW** 급 → **µW 예산과 3–4 자릿수 충돌**.
- 대책:
  1. **울트라 저전력 FPGA 계열** (예: Lattice iCE40 UltraPlus, Microchip PolarFire 계열
     — ⚠️ 실제 정적전력 **데이터시트 확인 필요**).
  2. **클록 게이팅 / 전력 게이팅** — 이벤트 없을 때 완전 정지 (AFE가 이벤트 기반이라 궁합 좋음).
  3. **FPGA는 프로토타입, 전력 수치는 ASIC 기준**으로 리포트 (Cerutti도 CUTIE ASIC 가정으로
     15.7 nJ/분류를 계산).

> **정직한 정리**: FPGA에서 "분류당 ≤15 mJ"는 쉽지만, Cerutti급 **µW 상시대기**는 FPGA
> 정적전력 때문에 어렵다. 목표를 (a) FPGA=기능 검증, (b) 전력=ASIC 추정으로 분리해야 한다.

## 3.5 추가 최적화 아이디어

| 아이디어 | 내용 | 기대 |
|---|---|---|
| **스트리밍/이벤트 구동** | 128프레임 버퍼링 대신 프레임 도착마다 처리 | 지연·버퍼↓, AFE 이벤트 특성과 정합 |
| **conv1/conv3 경량화** | 메모리 80%를 차지 → 채널수·커널 축소 또는 4-bit 양자화 | 메모리 최대 절반↓ |
| **가중치 압축** | 이진 가중치 희소성/반복성 활용 | BRAM↓ |
| **T 축소** | 현재 T=128 고정(conv2 span 57 제약) | conv2 커널 줄이면 T↓ 가능 → 연산·버퍼↓ |
| **레이어 융합** | BN+sign+pack을 한 파이프 단으로 | 레지스터↓ |

## 3.6 실행 순서

1. ~~연산량 정밀 카운트~~ ✅ **완료** — 6.01 MMAC/분류 (`scripts/count_ops.py`).
2. ~~folding 설계점 탐색~~ ✅ **완료** — balanced(64/8/4) = 5.2 ms, minimal도 30 ms.
3. **자원 추정 (다음)** — 위 설계점의 LUT/FF/BRAM/DSP를 산출.
   - DSP: `P_dsp`개(balanced=4) — int8 MAC.
   - BRAM: 가중치 47.55 KB → 소형 FPGA에서도 수십 블록 수준. **int8 38 KB가 지배**하므로
     conv1/conv3 4-bit 양자화를 먼저 검토하면 메모리가 절반 가까이 준다.
   - LUT: 64-lane XNOR+popcount 트리 ≈ 수백 LUT (⚠️ 해석적 추정 → **합성 검증 필요**).
4. **정적 전력 조사** — 후보 FPGA 계열의 정적전력 확인, µW 목표와의 격차 명시.
5. **export 경로 확정** — BN-임계 융합 + 비트팩킹 내보내기(`export/`)를 folding 구조에 맞춰 설계.

### 우선순위 제안 (실측 근거)

| 순위 | 조치 | 근거 |
|---|---|---|
| 1 | **완전펼침 → folding 전환** | 1e6× 과대. minimal조차 예산의 1/3 |
| 2 | **conv1/conv3 경량화**(채널·커널↓ 또는 4-bit) | 메모리 80%, MAC 65%를 차지 |
| 3 | **P_acc 우선 확대** | minimal의 병목이 conv1 누산 |
| 4 | 이진 pointwise 비트팩킹 최적화 | 이진 연산의 84% |
| 5 | 정적전력/전력게이팅 | µW 목표의 실질 결정 요인 |
