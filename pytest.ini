[pytest]
# Make `train`, `models`, `data`, ... importable without installing the package.
# Colab runs pytest from the repo root after `git pull`, so this must be explicit.
pythonpath = .
testpaths = tests
addopts = -q
